@model IEnumerable<Bookstore.Models.Book>
@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create" class="btn btn-outline-success">Create New</a>
</p>

<form asp-controller="Books" asp-action="Index" method="get">
@*      <input type="text" name="searchString" value="@ViewData["CurrentFilter"]" />
     <input type="submit" value="Search" /> *@
</form>

<table class="table">
    <thead>
        <tr>
            <th>Title</th>
            <th>Author</th>
            <th>Price</th>
            <th>Genre</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.Title</td>
                <td>@item.Author</td>
                <td>@item.Price</td>
                <td>@item.Genre</td>
                <td>
                    <a asp-action="Edit" asp-route-id="@item.BookId" class="btn btn-outline-info">Edit</a> 
                    <a asp-action="Details" asp-route-id="@item.BookId" class="btn btn-dark">Details</a> 
                    <a asp-action="Delete" asp-route-id="@item.BookId" class="btn btn-outline-secondary">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>